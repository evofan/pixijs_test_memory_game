// stage settings
export const STAGES: { WIDTH: number; HEIGHT: number; BG_COLOR: number } = {
  WIDTH: 440,
  HEIGHT: 440,
  BG_COLOR: 0x000000,
};

// path for assets
export const ASSETS: {
  ASSET_BG: string;
  ASSET_OBJ1: string;
} = {
  ASSET_BG: "assets/images/pic_bg_night.jpg",
  ASSET_OBJ1: "assets/images/cards.json",
};

export const GAMES: { FPS: number } = {
  FPS: 60, // framerate ex. 30
};
